# This file is intentionally empty.
# Workspace rules are now handled by profile-based files under
# ~/.config/hypr/profiles/{docked,laptop,external_only}/
# The active profile is sourced via:
#   source = ~/.config/hypr/profile-active/workspace-rules.conf
