# Host-specific packages for zeref
# Msi-gl-65

description: Packages specific to zeref

# Packages to install only on this host
packages:
  - nvidia-open-dkms
  - nvidia-utils
  - nvidia-settings
  - libva-nvidia-driver
  - libva-utils
  - vkd3d
  - mcontrolcenter-bin
# Packages to exclude from base or modules on this host
exclude: ["brltty"]
